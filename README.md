# Hozaak Music & Arts Fest - Next.js Rebuild 🚀

This repository contains the Next.js (React) rebuild of the Hozaak Music & Arts Festival landing page. The project has been restructured for modern development, improved maintainability, and enhanced UI/UX, ready for fast deployment on Vercel.

## ✨ Project Overview

The website is designed with a high-contrast, "Musical Vibe" aesthetic, using a custom component structure and centralized data management.

* **Framework:** Next.js (React)
* **Styling:** Pure CSS with CSS Variables (from `variables.css` and `global.css`)
* **Animations:** Uses a custom `useScrollAnimation` hook (Intersection Observer logic)
* **Deployment Target:** Vercel

---

## 🏗️ Folder Structure

The project is organized using best practices for scalability:
